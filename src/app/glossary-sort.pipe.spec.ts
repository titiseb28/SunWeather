import { GlossarySortPipe } from './glossary-sort.pipe';

describe('GlossarySortPipe', () => {
  it('create an instance', () => {
    const pipe = new GlossarySortPipe();
    expect(pipe).toBeTruthy();
  });
});
